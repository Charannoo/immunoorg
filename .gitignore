__pycache__/
*.py[cod]
*.pyd
*.pyo
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
.venv/
venv/
env/
*.log

# Evidence charts (PNGs) — committed so judges see them in the README.
# If you regenerate, just `python generate_evidence.py`.
# (Hugging Face Hub binary push works fine via the Hub Git LFS / Xet path
# `huggingface-cli upload` uses; the upload_to_hf.py script handles it.)

# Generated training artifacts (kept out of Hub git)
training/datasets/*.json.gz
training/grpo_data/*.json.gz
training/output/
training_runs/
training/trajectories/

# IDE / OS noise
.idea/
.vscode/
.DS_Store
Thumbs.db

# Locally-stored secrets (never commit)
.hf_token
.training_secret

